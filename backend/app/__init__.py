# app/__init__.py
#
# Marks the `app` directory as a Python package.
# This file is intentionally empty — its presence is what matters.
#
# In Python, a directory becomes a "package" (importable module) when it
# contains an __init__.py file. Without this file, Python would not allow
# `from app.models.lot import Lot` style imports.
